class Solution {
    public int findNumbers(int[] nums) {
        int count=0;
        for(int n:nums){
            int length=Integer.toString(n).length();
            if(length%2==0) count++;
        }
        return count;
    }
}
