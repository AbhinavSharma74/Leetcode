class Solution {
    public int countSegments(String s) {
        String[] str=s.split("\\s+");
        int count=0;

        for(String word:str){
            if(!word.isEmpty()){
                count++;
            }
        }
        return count;
    }
}
